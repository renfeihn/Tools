storm.local.hostname: ${storm_local_hostname}
storm.zookeeper.servers:${storm_zookeeper_servers}
storm.zookeeper.port: ${storm_zookeeper_port}
storm.zookeeper.root: "${storm_zookeeper_root}"
transactional.zookeeper.root: "${transactional_zookeeper_root}"
nimbus.seeds: [${nimbus_seeds}]
storm.local.dir: "${storm_home}/data/storm/storm-local"
storm.log.dir: "${storm_home}/logs/storm"
ui.port: ${ui_port}
logviewer.port: ${logviewer_port}
